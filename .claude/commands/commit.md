git addとcommitを行います。

以下の手順で実行してください：

1. `git status` で変更ファイルを確認する
2. `git diff --staged` と `git diff` で変更内容を確認する
3. 変更内容を踏まえて適切なコミットメッセージを日本語で作成する
4. `git add -A` ですべての変更をステージングする
5. `git commit -m "<メッセージ>"` でコミットする

コミットメッセージは変更内容を簡潔に表す日本語にしてください。
